# docker-compose.yml

services:
  # ... servicio mongo ...

  # ... servicio api ...

  # 🚨 SERVICIO FRONTEND 🚨
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
          # ✅ HTTPS para Railway
          NEXT_PUBLIC_API_URL: https://apitiendaonlineartelaser-production.up.railway.app
    container_name: nextjs_frontend
    ports:
      - "3000:3000"
    environment:
      # Variables de entorno para Next.js
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: "0.0.0.0"
      # ✅ HTTPS para Railway - Esta es la variable que usa el frontend
      NEXT_PUBLIC_API_URL: https://apitiendaonlineartelaser-production.up.railway.app
    restart: always
    # Si la API está en el mismo docker-compose, puedes usar networking
    # networks:
    #   - app-network

volumes:
  mongo_data:

# Si usas networking personalizado:
# networks:
#   app-network:
#     driver: bridge